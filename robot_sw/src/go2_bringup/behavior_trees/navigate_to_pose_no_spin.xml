<!--
  navigate_to_pose_no_spin.xml — the STOCK Nav2 navigate-to-pose tree with the
  in-place-rotation recovery removed. One deletion, nothing else.

  PROVENANCE (no invented tree):
    source : /opt/ros/jazzy/share/nav2_bt_navigator/behavior_trees/
             navigate_to_pose_w_replanning_and_recovery.xml
    deb    : ros-jazzy-navigation2 1.3.12-1noble.20260615.181551 (this repo's pin)
    sha256 : 5895b63840d54c6d7eee3d3b3f3ee177680af9e58a14cbf61c4df39fe5db2a90
             (of the stock file, extracted 2026-09-01 from the built image)

  DELTA vs stock — exactly one line deleted, from <RoundRobin name="RecoveryActions">:

      <Spin spin_dist="1.57" error_code_id="{spin_error_code}"/>

  WHY (measured, not a preference — platform decision AR-16 ①, evidence in
  reports/runner-2026-09-01-go2-c2b-wiring.md §6-1): this SUT's locomotion policy
  executes an IN-PLACE yaw command at **6 % of the commanded rate** (0.047 rad/s for a
  commanded 0.8), while the SAME policy turns at **91 %** when it is also walking
  (0.730 rad/s). The 4 control experiments in that report attribute this to the
  pre-trained flat checkpoint itself, not to the wiring: the command slots are in the
  right order, the policy does react to wz, and the angular observation matches
  ground-truth yaw rate.

  So `Spin 1.57` is a recovery this robot cannot perform: it would burn its whole
  `time_allowance` (BT default 10 s) rotating ~0.47 rad and then fail anyway, delaying
  the recoveries that DO work here (costmap clearing, wait, back-up) by that much every
  time the RoundRobin comes around. Deleting it is cheaper and more honest than tuning
  a behaviour whose actuator does not answer.

  The rest of the RoundRobin is untouched, so the recovery ladder is still
  clear-costmaps -> wait -> back-up, and the tree keeps the stock replanning structure.
  The other stock trees stay available; only `default_nav_to_pose_bt_xml` in
  ../params/nav2_params.yaml points here.
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
        <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
            <Sequence>
              <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="{selected_controller}" error_code_id="{follow_path_error_code}"/>
          <Sequence>
            <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
            <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
          </Sequence>
        </RecoveryNode>
      </PipelineSequence>
      <Sequence>
        <Fallback>
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
        </Fallback>
        <ReactiveFallback name="RecoveryFallback">
          <GoalUpdated/>
          <RoundRobin name="RecoveryActions">
            <Sequence name="ClearingActions">
              <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
            <Wait wait_duration="5.0"/>
            <BackUp backup_dist="0.30" backup_speed="0.15" error_code_id="{backup_code_id}"/>
          </RoundRobin>
        </ReactiveFallback>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
